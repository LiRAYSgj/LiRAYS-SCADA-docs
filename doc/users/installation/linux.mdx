import LinuxInstallSelector from '../../../src/components/LinuxInstallSelector.astro';

# Linux Installation

This guide covers package-based installation on Linux.

## Download and Install

Pick your target distribution and architecture. The command block and download link below update automatically.

<LinuxInstallSelector />

The installer registers the `lirays-scada.service` systemd service.

## Verify Service

```sh
systemctl status lirays-scada
journalctl -u lirays-scada -f
```

When the service is running, open:

- `http://127.0.0.1:8245`

Important paths:

- Config: `/etc/lirays-scada/settings.yaml`
- Data: `/var/lib/lirays-scada/data`

## Uninstall

Uninstall commands follow the distribution selector above.

<pre><code data-linux-uninstall-command>sudo apt remove lirays-scada</code></pre>

Optional full cleanup:

<pre><code data-linux-uninstall-cleanup>{`sudo apt purge lirays-scada
sudo rm -rf /etc/lirays-scada /var/lib/lirays-scada`}</code></pre>
